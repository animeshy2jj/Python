def calculatedistance(velocity, time=60):
    return velocity*time
print(calculatedistance(12))
print(calculatedistance(12,14))

#time is an default argument if no second value is passed it will take the default value
#if a value is passed for the second parameter it will take the passed value
